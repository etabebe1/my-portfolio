// component
import Layout from "./components/Layout";

function App({Component, pageProps}) {
  return (
    <div className="">
      djfhjags
      <Layout />
      {/* <Component {...pageProps}/> */}
    </div>
  );
}

export default App;
